<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.certicom.certifact_boletas_service_sp.mapper.AditionalFieldPaymentVoucherMapper">
    <insert id="save" parameterType="com.certicom.certifact_boletas_service_sp.model.AditionalFieldPaymentVoucherModel">
        INSERT INTO aditional_field_payment_voucher (id, nombre_campo, valor_campo, id_payment_voucher, type_field_id)
        VALUES (nextval('aditional_field_payment_voucher_seq'), #{nombreCampo}, #{valorCampo}, #{idPaymentVoucher}, #{typeFieldId})
    </insert>
    <delete id="deleteById" parameterType="Long">
        DELETE FROM aditional_field_payment_voucher WHERE id = #{id}
    </delete>
    <select id="findAllByIdPaymentVoucher" parameterType="Long">
        SELECT id, nombre_campo, valor_campo, id_payment_voucher, type_field_id FROM aditional_field_payment_voucher WHERE id_payment_voucher = #{id}
    </select>
</mapper>