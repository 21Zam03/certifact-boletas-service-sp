<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.certicom.certifact_boletas_service_sp.mapper.AnticipoPaymentVoucherMapper">
    <insert id="insert" parameterType="com.certicom.certifact_boletas_service_sp.model.AnticipoPaymentVoucherModel">
        INSERT INTO anticipo_payment_voucher (id_anticipo_payment, monto_anticipo, numero_anticipo, serie_anticipo, tipo_doc_anticipo, id_payment_voucher)
        values (nextval('anticipo_payment_voucher_seq'), #{montoAnticipo}, #{numeroAnticipo}, #{serieAnticipo}, #{tipoDocAnticipo}, #{idPaymentVoucher})
    </insert>
    <delete id="deleteById" parameterType="Long">
        DELETE FROM anticipo_payment_voucher WHERE id_anticipo_payment = #{id}
    </delete>
    <select id="findAllByIdPaymentVoucher" parameterType="Long">
        SELECT id_anticipo_payment, monto_anticipo, numero_anticipo, serie_anticipo, tipo_doc_anticipo, id_payment_voucher
        FROM anticipo_payment_voucher WHERE id_anticipo_payment = #{id}
    </select>
    <update id="update" parameterType="com.certicom.certifact_boletas_service_sp.model.AnticipoPaymentVoucherModel">
        UPDATE anticipo_payment_voucher
        set monto_anticipo = #{montoAnticipo},
            numero_anticipo = #{numeroAnticipo},
            serie_anticipo = #{serieAnticipo},
            tipo_doc_anticipo = #{tipoDocAnticipo},
            id_payment_voucher = #{idPaymentVoucher}
        WHERE id_anticipo_payment = #{idAnticipoPayment}
    </update>
    <update id="updateAll" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE anticipo_payment_voucher
            SET
            monto_anticipo   = #{item.montoAnticipo},
            numero_anticipo  = #{item.numeroAnticipo},
            serie_anticipo   = #{item.serieAnticipo},
            tipo_doc_anticipo = #{item.tipoDocAnticipo},
            id_payment_voucher = #{item.idPaymentVoucher}
            WHERE id_anticipo_payment = #{item.idAnticipoPayment}
        </foreach>
    </update>
    <select id="listAnticiposByIdPaymentVoucher" parameterType="Long">
        SELECT id_anticipo_payment, monto_anticipo, numero_anticipo, serie_anticipo, tipo_doc_anticipo, id_payment_voucher
        FROM anticipo_payment_voucher WHERE id_payment_voucher = #{id}
    </select>
    <insert id="insertAll" parameterType="java.util.List">
        INSERT INTO anticipo_payment_voucher (
            id_anticipo_payment,
            monto_anticipo,
            numero_anticipo,
            serie_anticipo,
            tipo_doc_anticipo,
            id_payment_voucher
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                nextval('anticipo_payment_voucher_seq'),
                #{item.montoAnticipo},
                #{item.numeroAnticipo},
                #{item.serieAnticipo},
                #{item.tipoDocAnticipo},
                #{item.idPaymentVoucher}
            )
        </foreach>
    </insert>
</mapper>