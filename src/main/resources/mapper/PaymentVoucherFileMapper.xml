<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.certicom.certifact_boletas_service_sp.mapper.PaymentVoucherFileMapper">
    <insert id="insert" parameterType="com.certicom.certifact_boletas_service_sp.model.PaymentVoucherFileModel">
        INSERT INTO payment_voucher_file (id, estado_archivo, orden, tipo_archivo, id_payment_voucher, id_register_file_send)
        values (nextval('file_comprobante_pago_seq'), #{estadoArchivo}, #{orden}, #{tipoArchivo}, #{idPaymentVoucher}, #{idRegisterFileSend})
    </insert>
    <select id="findAllByIdPaymentVoucher" parameterType="Long">
        SELECT id, estado_archivo, orden, tipo_archivo, id_payment_voucher, id_register_file_send FROM payment_voucher_file WHERE id_payment_voucher = #{id}
    </select>
    <update id="update" parameterType="com.certicom.certifact_boletas_service_sp.model.PaymentVoucherFileModel">
        UPDATE payment_voucher_file
        set estado_archivo = #{estadoArchivo},
        orden = #{orden},
        tipo_archivo = #{tipoArchivo},
        id_payment_voucher = #{idPaymentVoucher},
        id_register_file_send = #{idRegisterFileSend}
        WHERE id = #{id}
    </update>
    <update id="updateAll" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE payment_voucher_file
            SET
            estado_archivo = #{item.estadoArchivo},
            orden = #{item.orden},
            tipo_archivo = #{item.tipoArchivo},
            id_payment_voucher = #{item.idPaymentVoucher},
            id_register_file_send = #{item.idRegisterFileSend}
            WHERE id = #{item.id}
        </foreach>
    </update>
    <update id="deleteAll" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            DELETE FROM payment_voucher_file WHERE id = #{item.id}
        </foreach>
    </update>
    <insert id="insertAll" parameterType="java.util.List">
        INSERT INTO payment_voucher_file (
            id,
            estado_archivo,
            orden,
            tipo_archivo,
            id_payment_voucher,
            id_register_file_send
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                nextval('file_comprobante_pago_seq'),
                #{item.estadoArchivo},
                #{item.orden},
                #{item.tipoArchivo},
                #{item.idPaymentVoucher},
                #{item.idRegisterFileSend}
            )
        </foreach>
    </insert>
</mapper>