<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.certicom.certifact_boletas_service_sp.mapper.UserMapper">
    <select id="findById"
            parameterType="java.lang.Long"
            resultType="com.certicom.certifact_boletas_service_sp.model.UserModel">
        SELECT
            u.id_user,
            u.oficina_id,
            u.de_login,
            u.password,
            u.full_name,
            u.dni,
            u.estado,
            u.enabled,
            c.ruc
        FROM users u
        INNER JOIN company c ON u.cod_company = c.cod_company
        WHERE id_user = #{id}
    </select>

    <select id="findByUsername" parameterType="java.lang.String" resultType="com.certicom.certifact_boletas_service_sp.model.UserModel">
        SELECT
        u.id_user,
        u.dni,
        u.enabled,
        u.estado,
        u.full_name,
        u.password,
        u.typeUser,
        u.de_login,
        u.cod_company,
        u.oficina_id,
        u.change_pass,
        u.hidecomprobante,
        u.pdf_unico,
        u.date_last_seen_notifications,
        u.view_compra,
        c.ruc
        FROM users u
        INNER JOIN company c ON u.cod_company = c.cod_company
        WHERE de_login = #{username}
    </select>
</mapper>
